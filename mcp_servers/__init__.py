"""
MCP Server layer.

Wraps the existing tools/ package as MCP servers so any agent that speaks
the Model Context Protocol can discover and invoke them. Existing tool
implementations are imported, not modified.
"""
